using System;

namespace Matrizes {
    class Program {
        static void Main(string[] args) {

            //double[,] mat = new double[2, 3]; //Declaração de matriz

            //props e métodos
            //Console.WriteLine(mat.Length); //Mostra quantos elementos a matriz tem no total
            //Console.WriteLine(mat.Rank); //Mostra a quantidade de linhas
            //Console.WriteLine(mat.GetLength(0)); //Mostra a qtde de elementos na dimensão 0 (linha)
            //Console.WriteLine(mat.GetLength(1)); //Mostra a qtde de elementos na dimensão 1 (coluna)

        }
    }
}
